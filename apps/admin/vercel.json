{
  "version": 2,
  "installCommand": "npm install --prefix ../.. && npm install --prefix ../../packages/utils && npm install",
  "buildCommand": "npx prisma generate --schema=../../packages/db/prisma/schema.prisma && npm run build",
  "framework": null
}

