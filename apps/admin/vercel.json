{
  "version": 2,
  "installCommand": "npm install --prefix ../.. && npm install --prefix ../../packages/utils && npm install",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/algorithm-updates",
      "schedule": "0 9 * * 1"
    }
  ]
}

